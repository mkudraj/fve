{
  "name": "@fve/core",
  "version": "0.1.0",
  "description": "Core FACEIT match scouting logic - matchId detection, Data API client, roster parsing",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-test-module-mocks \"dist/**/*.test.js\"",
    "pretest": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0"
  }
}
